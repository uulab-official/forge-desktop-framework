# @forge/ipc-contract

Shared TypeScript IPC types and constants for Forge Desktop apps.

## What It Provides

- `WorkerRequest`, `WorkerResponse`, `WorkerProgress`
- `JobDefinition`, `JobStatus`
- `IPC_CHANNELS`
- protocol type guards

Use this package as the single source of truth for Electron-to-worker message shapes.
