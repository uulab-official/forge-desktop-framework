# @forge/resource-manager

Resolve resources, worker paths, and bundled binaries for Forge Desktop apps.

## What It Provides

- dev vs production path resolution
- worker executable discovery
- resource directory helpers for models, templates, and binaries

This package keeps Electron and worker path logic out of app code.
