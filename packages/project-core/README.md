# @forge/project-core

Project directory helpers and metadata management for Forge Desktop apps.

## What It Provides

- standard project directory creation
- project metadata loading and saving
- consistent `source/`, `analysis/`, `output/`, and `temp/` layout

Use it when your app manages user projects on disk.
