# @forge/logger

Structured logger with in-memory history for Forge Desktop apps.

## What It Provides

- `createLogger(source)`
- log history queries
- log entry subscriptions for consoles or debug panels

Use it for both app runtime logs and framework package diagnostics.
