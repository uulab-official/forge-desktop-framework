# @forge/error-handler

Friendly error mapping and React error boundaries for Forge Desktop apps.

## What It Provides

- `createErrorMapper()` for converting raw errors into user-facing messages
- `ForgeErrorBoundary` for renderer-side fallback UI

See the root API guide for examples in `docs/api-reference.md`.
