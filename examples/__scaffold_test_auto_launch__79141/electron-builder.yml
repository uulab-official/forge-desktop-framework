appId: com.forge.scaffoldtestautolaunch79141
productName: Scaffold Test Auto Launch 79141
directories:
  output: release
files:
  - dist/**/*
  - dist-electron/**/*
extraResources:
  - from: resources/
    to: resources/
  - from: worker/dist/
    to: resources/worker/

publish:
  - provider: github
    owner: ${env.GH_OWNER:-your-github-org-or-user}
    repo: ${env.GH_REPO:-scaffoldtestautolaunch79141}
    private: true
    token: ${env.GH_TOKEN}

mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  category: public.app-category.developer-tools
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  notarize:
    teamId: ${env.APPLE_TEAM_ID}

win:
  target: nsis
  signingHashAlgorithms:
    - sha256

linux:
  target: AppImage
