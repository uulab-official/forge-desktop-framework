# In development (from monorepo root):
# pip install -e ../../packages/worker-runtime
# No deps required for basic usage (video_info, thumbnail).
# ffmpeg/ffprobe must be installed on the system.
#
# For transcription, add:
# openai-whisper
