# Release Checklist: v0.1.124

- Status: ready
- Date: 2026-04-04
- Bump Type: patch

## Scope

- Summary: Add generated `ops:retention` support to the `production-ready` starter so repeated production audits prune stale operator snapshots and evidence directories instead of letting them grow unbounded.
- User-facing change: `production-ready` scaffolded apps now include `pnpm ops:retention`, generated workflows run it before fresh ops snapshots and evidence collection, and production smoke tests validate that only the newest evidence remains after pruning.

## Public Surface

- Docs: Update root docs, getting started, deployment, and CLI README to describe `pnpm ops:retention` as part of the production-ready operating model.
- Scripts and release flow: Add generated `scripts/ops-retention.sh`, wire `pnpm ops:retention` into generated validate and tagged release workflows, and teach release-surface audit plus smoke suites to verify retention behavior.
- CLI or scaffold surface: `create-forge-desktop --preset production-ready` now emits a generated app with bounded `ops/snapshots/` and `ops/evidence/` lifecycle management.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: Yes. `0.1.124` documents production ops retention for generated apps.
- Follow-up risks: Retention currently prunes by newest directory name ordering; if snapshot naming changes later, keep lexical sort aligned with timestamped directory names.
- Next target: Production-grade evidence indexing or retention reporting so generated apps can summarize what was pruned after each audit run.
