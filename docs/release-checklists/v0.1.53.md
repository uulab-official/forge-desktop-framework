# Release Checklist: v0.1.53

- Status: ready
- Date: 2026-03-30
- Bump Type: patch

## Scope

- Summary: add a publish channel parity audit so tagged releases can detect GitHub and S3 output drift
- User-facing change: maintainers can now fail tagged releases when GitHub and S3 channels disagree on installer names or updater manifest targets

## Public Surface

- Docs: README, deployment guide, CLI package README, AGENTS, and Codex project notes
- Scripts and release flow: add publish channel parity audit plus smoke test, wire it into `release:ship`, and run it in the tagged workflow when S3 publishing is enabled
- CLI or scaffold surface: none

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] `pnpm release:channels:test`
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: the parity audit only runs in the tagged workflow when `S3_ENABLED=true`, so teams without S3 enabled still rely on the synthetic smoke
- Next target: teach the tagged workflow to summarize channel parity alongside the matrix summary when S3 publishing is active
