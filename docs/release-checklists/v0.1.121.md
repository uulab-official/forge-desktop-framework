# Release Checklist: v0.1.121

- Status: ready
- Date: 2026-04-04
- Bump Type: patch

## Scope

- Summary: Add a generated operations snapshot surface for production-grade starters and verify it in scaffold audits and smoke flows.
- User-facing change: Generated apps now ship `pnpm ops:snapshot`, produce Markdown and JSON operator snapshots under `ops/snapshots/`, and include that command in production-ready docs and CI workflows.

## Public Surface

- Docs: Update root README, getting-started, deployment, and CLI README to document `ops:snapshot` as part of the production-ready baseline.
- Scripts and release flow: Add generated `scripts/ops-snapshot.sh`, wire it into generated validate and release workflows, and extend release-surface audit plus production-ready smoke coverage.
- CLI or scaffold surface: Add `ops:snapshot` to generated package scripts, release checklist guidance, release playbook, and production-readiness guide.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: keep the operator snapshot compact so it stays useful instead of becoming another release artifact dump
- Next target: production-grade runtime evidence export or release workflow artifact upload for generated `ops:snapshot` output
