# Release Checklist: v0.1.136

- Status: ready
- Date: 2026-04-07
- Bump Type: patch

## Scope

- Summary: Add generated `ops:recover` so `production-ready` apps can turn the latest restore rehearsal into a final recovery handoff record.
- User-facing change: `production-ready` apps now ship `pnpm ops:recover`, retain `ops/recoveries/`, and upload recovery artifacts from validate and tagged release workflows.

## Public Surface

- Docs: Update root docs plus create-forge-app docs to describe `ops:recover`, `ops/recoveries/`, and the production recovery rehearsal flow.
- Scripts and release flow: Extend generated validate and release workflows, release-surface audit, and internal or external production smoke checks to include recovery artifacts.
- CLI or scaffold surface: Scaffold `scripts/ops-recover.sh`, package scripts, inventory metadata, retention, and generated production playbooks.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: `ops:recover` depends on the latest `ops:restore` payload shape, so future payload changes still need the same surface audit and smoke coverage.
- Next target: add a production-facing `ops:rollback` or equivalent operator drill on top of the new recovery surface.
