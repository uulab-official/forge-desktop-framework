# Release Checklist: v0.1.137

- Status: ready
- Date: 2026-04-07
- Bump Type: patch

## Scope

- Summary: Add generated `ops:rollback` to the `production-ready` starter so operator recovery proof can be elevated into a final rollback verdict.
- User-facing change: Scaffolded production-grade apps now emit `ops/rollbacks/` records, upload them in validate and tagged release workflows, and keep them bounded with retention.

## Public Surface

- Docs: Update framework and CLI docs so `production-ready` explicitly documents `ops:rollback` and `ops/rollbacks/`.
- Scripts and release flow: Generated validate and tagged release workflows now run `pnpm ops:rollback` and upload `ops/rollbacks/` artifacts.
- CLI or scaffold surface: `production-ready` apps now include `pnpm ops:rollback` and `scripts/ops-rollback.sh`.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: generated `ops:rollback` must stay bash 3.2 compatible because `production-ready` smoke runs on macOS maintainers too
- Next target: keep pushing production-grade operator surfaces, likely around rollback attestations or rollback handoff exports
