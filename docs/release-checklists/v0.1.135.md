# Release Checklist: v0.1.135

- Status: ready
- Date: 2026-04-06
- Bump Type: patch

## Scope

- Summary: add generated `pnpm ops:restore` so `production-ready` apps can rehydrate the latest offline export and leave a final restore rehearsal record under `ops/restores/`
- User-facing change: generated workflows, docs, retention, and production smoke now treat restore rehearsal as the final offline handoff proof on top of the existing export surface

## Public Surface

- Docs: `README.md`, `docs/getting-started.md`, `docs/deployment.md`, `packages/create-forge-app/README.md`, generated `docs/release-playbook.md`, and generated `docs/production-readiness.md` now describe `ops:restore` and `ops/restores/`
- Scripts and release flow: generated apps now ship `scripts/ops-restore.sh`, validate or tagged release workflows run `pnpm ops:restore`, upload `ops/restores/`, and retention or indexing keeps restore rehearsal output bounded
- CLI or scaffold surface: `create-forge-desktop` generated `production-ready` apps now expose `pnpm ops:restore` in `package.json`, README command lists, and release checklist text

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes, `0.1.135` documents the new production restore rehearsal layer
- Follow-up risks: restore rehearsal currently validates the latest export payload from local disk; next step should make operator-facing summary and handoff flows consume the restore verdict directly
- Next target: production-grade `ops:finalize` or equivalent command that rolls restore verdict back into one final operator-facing summary
