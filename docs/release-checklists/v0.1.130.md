# Release Checklist: v0.1.130

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add `ops:ready` to scaffolded `production-ready` apps as the single production-grade orchestration command that refreshes the full ops chain and emits a final ready verdict under `ops/ready/`.
- User-facing change: Generated apps now ship `pnpm ops:ready`, generated validate and tagged release workflows upload `ops/ready/`, and production-ready documentation now treats `ops:ready` as the primary operator command above snapshot/evidence/report/bundle/index/doctor/handoff.

## Public Surface

- Docs: Root README, getting-started guide, deployment guide, CLI README, generated release playbook, and generated production-readiness guide now describe `ops:ready` and `ops/ready/`.
- Scripts and release flow: `audit-release-surfaces.sh`, internal/external `production-ready` scaffold smoke, generated validate/release workflows, and production-readiness flows now include ready-pipeline coverage.
- CLI or scaffold surface: `create-forge-desktop` now scaffolds `scripts/ops-ready.sh`, `pnpm ops:ready`, `ops/ready/`, and production-readiness automation that runs the one-shot ready pipeline.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: Keep `ops:ready`, `production:check:*`, workflow artifacts, and retention semantics aligned if future ops surfaces are added above handoff.
- Next target: Add a production-grade `ops:publish` or `ops:ready-index` surface that summarizes the newest ready verdict for operators and CI artifacts.
