# Release Checklist: v0.1.68

- Status: ready
- Date: 2026-04-01
- Bump Type: patch

## Scope

- Summary: Add a stable recovery-command summary artifact on top of the provider-agnostic remote rollback drill flow.
- User-facing change: Maintainers now get a single `recovery-command-summary.md/json` file that captures the chosen rollback target, archived assets, rerun command, and recommended next actions after a remote rollback drill completes.

## Public Surface

- Docs: README, deployment guide, CLI package README, AGENTS notes, Codex project notes, and changelog updated for the new recovery summary artifact.
- Scripts and release flow: Added a recovery summary generator and wired the provider-agnostic remote rollback drill wrapper to emit it automatically.
- CLI or scaffold surface: No scaffold change; maintainer-side release recovery tooling only.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: The recovery summary assumes the remote rollback drill payload remains the source of truth for provider options and selected bundle metadata; future wrapper changes need to keep that contract stable.
- Next target: Freeze and document the explicit `1.0` release gate so maintainer recovery automation and public preset stability share one final milestone.
