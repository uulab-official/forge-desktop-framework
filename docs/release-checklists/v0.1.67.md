# Release Checklist: v0.1.67

- Status: ready
- Date: 2026-04-01
- Bump Type: patch

## Scope

- Summary: Add a provider-agnostic remote rollback drill command that closes fetch, prepare, and drill execution in one maintainer flow.
- User-facing change: Maintainers can run the full remote recovery path from GitHub or S3 without manually chaining separate history fetch, preparation, and rollback drill commands.

## Public Surface

- Docs: README, deployment guide, CLI package README, AGENTS notes, Codex project notes, and changelog updated for the final remote rollback drill flow.
- Scripts and release flow: Added a provider-agnostic remote rollback drill wrapper and smoke test, wired them into release gates and CI release-readiness.
- CLI or scaffold surface: No scaffold change; maintainer-side release recovery tooling only.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: The provider-agnostic wrapper assumes provider-specific history fetch helpers continue to preserve the same prepared output contract and that the current release directory already contains valid rollback playbook and recovery metadata.
- Next target: Add structured rollback execution output that can be archived alongside release bundles so recovery attempts become traceable artifacts too.
