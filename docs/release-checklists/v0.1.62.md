# Release Checklist: v0.1.62

- Status: ready
- Date: 2026-03-31
- Bump Type: patch

## Scope

- Summary: Mirror archived release bundles into object storage and add a maintainer helper that can fetch those bundles from S3 or R2 before a rollback drill.
- User-facing change: Framework maintainers can now reconstruct rollback drill input from the S3 release bucket without first downloading GitHub Actions artifacts.

## Public Surface

- Docs: Update repo README, deployment guide, and CLI package README with the S3 bundle mirror and fetch workflow.
- Scripts and release flow: Add `fetch-release-inventory-bundle-from-s3.sh`, its smoke test, a new `release:bundle:fetch:s3:test` gate, and mirror release bundle metadata into S3 during tagged releases.
- CLI or scaffold surface: No generated app surface change; this is maintainer-only release tooling.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: The S3 helper still depends on the bucket retaining `release-bundles/vX.Y.Z/` prefixes and the maintainer having valid `aws` credentials or endpoint settings.
- Next target: remote bundle fetch parity for release-bundle metadata caching across providers, or automated rollback target selection from the mirrored bundle index.
