# Release Checklist: v0.1.61

- Status: ready
- Date: 2026-03-31
- Bump Type: patch

## Scope

- Summary: Add a maintainer helper that fetches tagged GitHub Actions release artifacts with `gh`, restores the archived bundle index locally, and then reuses the canonical bundle retrieval flow.
- User-facing change: Framework maintainers can now reconstruct rollback drill input from tagged GitHub release artifacts without manual artifact browsing or ad hoc downloads.

## Public Surface

- Docs: Update repo README, deployment guide, and CLI package README with the remote bundle fetch flow.
- Scripts and release flow: Add `fetch-release-inventory-bundle-from-github.sh`, its smoke test, and a new `release:bundle:fetch:test` release gate.
- CLI or scaffold surface: No generated app surface change; this is maintainer-only release tooling.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: Real `gh` auth and GitHub artifact retention policy still govern whether archived bundles remain fetchable across older releases.
- Next target: archived bundle remote fetch helper for non-GitHub storage, or a remote bundle cache/index sync path that can seed rollback drills without re-downloading every artifact.
