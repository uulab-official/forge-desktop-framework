# Release Checklist: v0.1.122

- Status: ready
- Date: 2026-04-04
- Bump Type: patch

## Scope

- Summary: Preserve production operations snapshots as generated GitHub Actions artifacts and enrich snapshot payloads with CI execution context.
- User-facing change: Generated validate and tagged release workflows now upload `ops/snapshots/`, and snapshot JSON/Markdown now include workflow, run, ref, SHA, and runner context.

## Public Surface

- Docs: Update root README, getting-started, deployment, and CLI README to document snapshot artifact retention in generated workflows.
- Scripts and release flow: Extend generated validate and release workflows with `actions/upload-artifact@v4` for `ops/snapshots/`, and enrich the generated snapshot script with CI context metadata.
- CLI or scaffold surface: Keep `ops:snapshot` as the generated production-ready surface and document that its output is now preserved by default in CI and tagged release runs.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: keep generated workflow artifact retention lightweight so snapshot evidence stays useful without bloating release runs
- Next target: generated incident or support bundle upload path for production-ready CI runs
