# Release Checklist: v0.1.64

- Status: ready
- Date: 2026-03-31
- Bump Type: patch

## Scope

- Summary: Add a remote release history index that aggregates multiple fetched tag directories for maintainer recovery work.
- User-facing change: Maintainers can inspect cached archived bundles across versions instead of one tag at a time, and fetch helpers keep that history index fresh automatically.

## Public Surface

- Docs: README, deployment guide, CLI package README, AGENTS notes, Codex project notes, and changelog updated for release history indexing.
- Scripts and release flow: Added history index generator and smoke test, wired it into release gates, CI release-readiness, and both remote fetch helpers.
- CLI or scaffold surface: No scaffold change; maintainer-side release recovery tooling only.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: History indexing assumes fetched tag directories keep a stable `release-bundle-index.json` and `release-provenance.json` shape.
- Next target: Build automatic rollback retrieval on top of the new multi-tag history index and selector chain.
