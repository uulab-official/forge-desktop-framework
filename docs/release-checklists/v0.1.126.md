# Release Checklist: v0.1.126

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add generated `ops:report` to the `production-ready` starter so scaffolded apps emit one consolidated operator-facing report and upload it from CI and tagged releases.
- User-facing change: `forge create --preset production-ready` apps now include `pnpm ops:report`, generated `scripts/ops-report.sh`, `ops/reports/` retention, and workflow artifact upload for consolidated ops reports.

## Public Surface

- Docs: README, getting-started, deployment guide, CLI README, changelog, and this release checklist all describe `ops:report` as part of the production-grade workflow.
- Scripts and release flow: Generated validate and tagged release workflows run `pnpm ops:report` and upload `ops/reports/`; generated `ops:retention` also prunes `ops/reports/`.
- CLI or scaffold surface: `production-ready` scaffolds now include `pnpm ops:report` and `scripts/ops-report.sh` next to the existing snapshot/evidence/index commands.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: `ops:report` depends on latest snapshot/evidence/index shape, so future script changes should keep those JSON surfaces backward compatible.
- Next target: Add indexed retention or signed evidence packaging for production-ready operator artifacts.
