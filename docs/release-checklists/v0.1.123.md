# Release Checklist: v0.1.123

- Status: ready
- Date: 2026-04-04
- Bump Type: patch

## Scope

- Summary: Add a generated `ops:evidence` bundle command so production-ready apps can preserve a reusable operator evidence pack alongside snapshots.
- User-facing change: Generated apps now include `pnpm ops:evidence`, production checks emit evidence bundles, and generated validate or release workflows upload `ops/evidence/` artifacts.

## Public Surface

- Docs: Update root README, getting-started, deployment, and CLI README to document the new evidence bundle command and workflow artifacts.
- Scripts and release flow: Add generated `scripts/ops-evidence.sh`, wire `production-readiness.sh` and generated GitHub workflows to emit and upload evidence bundles, and extend release-surface audits to require the new path.
- CLI or scaffold surface: Keep `production-ready` as the production baseline and extend generated package scripts, docs, and `.gitignore` with `ops:evidence` plus `ops/evidence/`.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: keep the evidence bundle lightweight by copying manifests and docs rather than installer binaries so CI artifact size stays predictable.
- Next target: add production-facing retention or indexing around `ops/evidence/` so repeated evidence capture stays manageable.
