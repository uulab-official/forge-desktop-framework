# Release Checklist: v0.1.125

- Status: ready
- Date: 2026-04-04
- Bump Type: patch

## Scope

- Summary: Add generated `ops:index` support to the `production-ready` starter so operators can inspect current retained snapshot and evidence state in one Markdown and JSON inventory.
- User-facing change: `production-ready` scaffolded apps now include `pnpm ops:index`, generated workflows upload `ops/index/`, and smoke coverage verifies the index exists after evidence generation and retention.

## Public Surface

- Docs: Update product docs and CLI docs to include `pnpm ops:index` in the production-grade operating path.
- Scripts and release flow: Add generated `scripts/ops-index.sh`, upload `ops/index/` in generated validate and tagged release workflows, and extend retention to prune old index directories too.
- CLI or scaffold surface: `create-forge-desktop --preset production-ready` now emits a generated app with a first-class operations inventory command and artifact path.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [x] `pnpm release:ship patch`
- [x] `pnpm version:check`

## Release Notes

- Changelog entry drafted: Yes. `0.1.125` documents production ops index support for generated apps.
- Follow-up risks: The ops index currently inventories local directories only; if teams need cross-run or remote aggregation, add a follow-up exporter instead of overloading the local command.
- Next target: Production evidence retention reporting or artifact index export for CI and release workflows.
