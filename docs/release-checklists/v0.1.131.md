# Release Checklist: v0.1.131

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add `ops:attest` to the generated `production-ready` operator evidence chain and keep workflows, retention, index, and audits aligned.
- User-facing change: Scaffolded `production-ready` apps now expose `pnpm ops:attest` and upload retained `ops/attestations/` evidence from generated validate and tagged release workflows.

## Public Surface

- Docs: Root README, getting-started, deployment guide, create-forge-app README, changelog, and this checklist describe `ops:attest` as part of the production-ready operator evidence flow.
- Scripts and release flow: Generated apps now ship `scripts/ops-attest.sh`, include `ops:attest` in package scripts, upload `ops/attestations/`, and keep it bounded through retention and smoke coverage.
- CLI or scaffold surface: `production-ready` scaffold output now includes `ops:attest`, refreshed `ops:ready`, updated `ops:index`, and retention for `ops/attestations/`.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: Keep `ops:attest` aligned with future operator surfaces so hashes and workflow artifact paths do not drift.
- Next target: Push production-ready further by turning operator evidence into a final export surface that downstream incident tooling can ingest directly.
