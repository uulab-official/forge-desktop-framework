# Release Checklist: v0.1.138

- Status: ready
- Date: 2026-04-07
- Bump Type: patch

## Scope

- Summary: Add a final generated `ops:incident` escalation packet on top of the production-ready rollback chain.
- User-facing change: `production-ready` scaffolded apps now generate `ops/incidents/` with Markdown, JSON, and tarball incident-response bundles.

## Public Surface

- Docs: Root docs, deployment docs, getting-started docs, and create-forge-app README mention `ops:incident` and `ops/incidents/`.
- Scripts and release flow: Generated validate and release workflows now run `pnpm ops:incident` and upload `ops/incidents/`.
- CLI or scaffold surface: `create-forge-desktop` generated `production-ready` apps now include `pnpm ops:incident` and `scripts/ops-incident.sh`.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: Generated packet format now spans rollback, export, handoff, and release evidence, so future ops-surface renames need incident-packet coverage in the same pass.
- Next target: Add a production-grade rollback escalation index or attested incident handoff on top of `ops:incident`.
