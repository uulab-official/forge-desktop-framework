# Release Checklist: v0.1.129

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add `ops:handoff` to scaffolded `production-ready` apps as the final operator handoff surface built on top of doctor, bundle, report, docs, env template, and release manifests.
- User-facing change: Generated apps now ship `pnpm ops:handoff`, upload `ops/handoffs/` in validate and release workflows, and retain handoff directories alongside other ops evidence.

## Public Surface

- Docs: Root README, getting-started guide, deployment guide, CLI README, generated release playbook, and generated production-readiness guide now describe `ops:handoff`.
- Scripts and release flow: `audit-release-surfaces.sh`, internal/external production-ready scaffold smoke, generated validate/release workflows, `ops:index`, and `ops:retention` now include handoff coverage.
- CLI or scaffold surface: `create-forge-desktop` now scaffolds `scripts/ops-handoff.sh`, `pnpm ops:handoff`, `ops/handoffs/`, and production-readiness automation that runs handoff after doctor.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: Keep `ops:index` and retention aligned if future ops surfaces are added above handoff.
- Next target: Add a production-grade `ops:publish` or `ops:handoff-index` surface that summarizes the newest handoff for operators and CI artifacts.
