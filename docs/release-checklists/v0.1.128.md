# Release Checklist: v0.1.128

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add `ops:doctor` to the generated `production-ready` operator surface.
- User-facing change: Scaffolded production-ready apps now emit a final Markdown and JSON ops verdict under `ops/doctors/` and upload it from generated validate and tagged release workflows.

## Public Surface

- Docs: Root README, getting-started guide, deployment guide, CLI README, generated production-readiness docs, and changelog now describe `ops:doctor`.
- Scripts and release flow: Generated validate and release workflows run `pnpm ops:doctor` and upload `ops/doctors/`; release-surface audit and scaffold smoke now verify that surface.
- CLI or scaffold surface: `create-forge-desktop` now generates `scripts/ops-doctor.sh`, `pnpm ops:doctor`, `ops/doctors/`, and doctor coverage in generated indexes and retention.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: `ops:index` inventories doctor outputs once rerun after doctor generation, so the default flow continues to treat doctor as the last handoff artifact rather than feeding it back into the same index pass.
- Next target: Add a production-grade export or publish-side consumer for the doctor verdict so operator evidence can be handed off as one higher-level release packet.
