# Release Checklist: v0.1.134

- Status: ready
- Date: 2026-04-05
- Bump Type: patch

## Scope

- Summary: Add a final portable `ops:export` surface to the generated `production-ready` starter and wire it into workflows, retention, audits, smoke coverage, and docs.
- User-facing change: `production-ready` scaffolded apps now generate `ops/exports/` with Markdown, JSON, and tarball exports for offline operator handoff after the final release pack.

## Public Surface

- Docs: Root README, getting-started, deployment guide, and create-forge-app README now document `pnpm ops:export` and `ops/exports/`.
- Scripts and release flow: Release-surface audit plus internal and external scaffold smoke now verify export generation, artifact upload, and retention alongside the existing release pack.
- CLI or scaffold surface: Generated `production-ready` apps now include `pnpm ops:export`, `scripts/ops-export.sh`, workflow artifact uploads for `ops/exports/`, and retention or index coverage for the new directory.

## Validation Plan

- [x] `pnpm --filter create-forge-desktop build`
- [x] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: Yes
- Follow-up risks: Validate the generated `ops-export` archive shape against real signed and notarized package outputs, not just seeded smoke artifacts.
- Next target: Add an operator-facing promotion or publish handoff surface on top of the final export for production sign-off.
