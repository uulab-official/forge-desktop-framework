# Release Checklist: v0.1.65

- Status: ready
- Date: 2026-04-01
- Bump Type: patch

## Scope

- Summary: Add a one-command rollback preparation helper that selects and retrieves a rollback bundle from a multi-tag release history root.
- User-facing change: Maintainers can turn an accumulated cached history root into a drill-ready archived bundle without manually stitching selector and retrieval commands together.

## Public Surface

- Docs: README, deployment guide, CLI package README, AGENTS notes, Codex project notes, and changelog updated for history-root rollback preparation.
- Scripts and release flow: Added a history-root rollback preparation helper and smoke test, wired it into release gates and CI release-readiness.
- CLI or scaffold surface: No scaffold change; maintainer-side release recovery tooling only.

## Validation Plan

- [ ] `pnpm --filter create-forge-desktop build`
- [ ] Narrow feature-specific smoke tests
- [ ] `pnpm release:ship patch`
- [ ] `pnpm version:check`

## Release Notes

- Changelog entry drafted: yes
- Follow-up risks: History-root preparation assumes each fetched tag preserves a valid `release-history-index.json` input shape with stable `releaseRoot` and bundled inventory metadata.
- Next target: Extend the accumulated history root into remote multi-version rollback retrieval so GitHub or S3 fetch plus history-root preparation can close in one maintainer command.
